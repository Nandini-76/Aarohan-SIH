"""
App package for FastAPI backend.

This file marks the directory as a Python package so that imports like
`from app.utils import ...` work when running the server as
`uvicorn app.main:app` from the backend root.
"""
